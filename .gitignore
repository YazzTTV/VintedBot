# --- Secrets & config locale ---
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
credentials.json
**/secrets/

# --- Données opérationnelles (listings temporaires à la racine) ---
/Input_Screenshots/
/Output_Listings/
/Products_Archive/
/vinted_bot/Fake_Bank/
*.xlsx
*.xls

# --- Profil navigateur Edge (cookies, sessions, email) ---
/vinted_bot/Bot_Profile/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/

# --- Node / Next.js (vinted-manager) ---
node_modules/
.next/
out/
build/
.vercel/
*.tsbuildinfo
next-env.d.ts
/vinted-manager/src/generated/

# --- Debug & artefacts locaux ---
/vinted_bot/debug_*.png
/vinted_bot/risk_page_content.html
/vinted_bot/raw_response.txt
# Sorties de generation d'annonces (images IA temporaires par compte)
/vinted_bot/_test_out_*/
/vinted_bot/test_nano_selfie.jpg
# Dossier dezippe duplique a la racine
/VintedBot-main/
# Dossiers de profils Brave dedies (sessions) - ne jamais committer
/BraveBot/

# --- OS & IDE ---
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
*.log
**/.stfolder/

# --- Prisma local ---
/prisma/*.db
/prisma/*.db-journal

# Sorties de la routine de publication en salves (images IA generees par compte)
/vinted_bot/_salve_out/
# Etat runtime de la routine (mute a chaque execution -> conflits si versionne)
/vinted_bot/routine_state.json
